import random

health = 50

difficulty = 1

potion = int (random.randint(25,50) / difficulty)

health = health + potion

print(health)


"python uses casting to convert one data type into another as in the int to convert the float."
"python has  a vast library as I imported the random function from the library."
